---
import type { KnownLocale } from '../../types';

interface Props {
    locale: KnownLocale;
}

const { locale } = Astro.props;
---
<div class="support-landing">
    <h1>Support Landing ({locale})</h1>
    <p>This is the support landing page.</p>
</div>
